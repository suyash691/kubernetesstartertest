apiVersion: v1
kind: Service
metadata:
    name: "testabcd-feff"
    labels:
        app: "testabcd-feff"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "testabcd-feff"